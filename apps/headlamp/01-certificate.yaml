---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: headlamp-certificate
  namespace: headlamp
spec:
  secretName: headlamp-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  commonName: headlamp.${CLUSTER_DOMAIN}
  dnsNames:
    - headlamp.${CLUSTER_DOMAIN}