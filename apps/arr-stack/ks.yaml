apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: apps-arr-stack
  namespace: flux-system
spec:
  interval: 5m
  path: ./polaris
  prune: true
  sourceRef:
    kind: GitRepository
    name: arr-stack
  postBuild:
    substituteFrom:
      - kind: ConfigMap
        name: cluster-vars