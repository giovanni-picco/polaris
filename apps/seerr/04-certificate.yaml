---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: seerr-certificate
  namespace: seerr
spec:
  secretName: seerr-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  commonName: seerr.${CLUSTER_DOMAIN}
  dnsNames:
    - seerr.${CLUSTER_DOMAIN}