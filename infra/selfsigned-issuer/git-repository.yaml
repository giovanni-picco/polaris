---
apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: selfsigned-issuer
  namespace: flux-system
spec:
  interval: 1m0s
  url: https://gitlab.com/gp-homelab/k8s/selfsigned-issuer.git
  ref:
    branch: main
  secretRef:
    name: gitlab-deploy-token
