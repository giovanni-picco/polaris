---
apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: node-feature-discovery
  namespace: flux-system
spec:
  interval: 1m0s
  url: https://gitlab.com/gp-homelab/k8s/node-feature-discovery.git
  ref:
    branch: main
  secretRef:
    name: gitlab-deploy-token
